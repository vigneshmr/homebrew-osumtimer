# homebrew-osumtimer

Homebrew tap for [OsumTimer](https://github.com/vigneshmr/osumtimer), a menu bar timer for macOS.

```sh
brew tap vigneshmr/osumtimer
brew install osumtimer
```

Casks here are written by `make release` in the app repo — edit them there.
